---
language: typescript
extensions: [".ts", ".tsx", ".js", ".jsx"]
---

# TypeScript documentation rules

- Flag new exported functions/types in scope without TSDoc/JSDoc
- README must mention `tsc`, `eslint`, or package manager when TS tooling documented
- Flag `@param` / `@returns` mismatch with actual signatures in changed files
