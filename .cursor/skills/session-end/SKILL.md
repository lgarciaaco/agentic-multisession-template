# Session end

Triggers: `end session`, `/end-session`

Hook does not close sessions — only this flow.

1. `./scripts/resolve-session.sh` or user codename
2. `./scripts/end-session.sh [codename] [note]`
